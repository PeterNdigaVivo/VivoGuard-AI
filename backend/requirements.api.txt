# API-side dependencies. NO torch, NO ultralytics, NO opencv.
# Worker image extends this set with requirements.worker.txt.
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
# Required by pydantic.EmailStr (used in auth login schema). Without it
# the API crashes on import with "ImportError: email-validator is not installed".
email-validator>=2.1
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
redis>=5.0
celery[redis]>=5.4
boto3>=1.34
minio>=7.2
# --- Password hashing ---
# Direct bcrypt — no passlib. We previously fought passlib's
# bcrypt.__about__ probe (broken in bcrypt 4.1+); switching to the
# bcrypt library directly removes both the version constraint and the
# whole class of resolver bugs that kept reinstalling bcrypt 5.x and
# breaking login. Any modern bcrypt works.
bcrypt>=4.0
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
cryptography>=42
httpx>=0.27
onvif-zeep-async>=3.1
WSDiscovery>=2.1
aiosmtplib>=3.0
twilio>=9.0
websockets>=12
Pillow>=10
# PDF report generation for retail analytics (P4). reportlab has no
# system dependencies so it works in the slim API image without bloat.
reportlab>=4.0
# Excel export for the alert-history report (manager review). Pure
# Python, no system deps.
openpyxl>=3.1
